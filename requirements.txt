# easyAgent 依赖包

# 核心依赖
pydantic>=2.0.0
openai>=1.0.0
pydantic-settings>=2.0.0  # 配置管理

# MCP集成依赖
aiohttp>=3.9.0  # 用于SSE传输（远端MCP服务器）

# API服务依赖
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
aiofiles>=23.0.0  # 异步文件操作

# 其他有用的库
python-dotenv>=1.0.0  # 用于环境变量管理
reportlab>=4.0.0      # 用于PDF导出功能
markdown>=3.5.0       # 用于Markdown渲染
python-docx>=1.0.0    # 用于Word文档导出功能
requests>=2.31.0      # 用于HTTP请求（网页爬虫）
beautifulsoup4>=4.12.0  # 用于HTML解析（网页爬虫）
# playwright>=1.30.0    # 用于支持JavaScript渲染的网页爬虫（可选）

# 开发依赖（可选）
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.0.0
