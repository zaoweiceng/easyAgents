# easyAgent 配置文件示例
# 复制此文件为 .env 并填入实际配置

# ============================================
# LLM服务配置
# ============================================

# LLM服务地址
# 示例:
#   - OpenAI官方: https://api.openai.com/v1
#   - Azure OpenAI: https://your-resource.openai.azure.com/
#   - 本地vLLM: http://localhost:8000/v1
#   - 其他兼容服务
LLM_BASE_URL=http://127.0.0.1:9999/v1

# LLM API密钥
# 重要: 请勿将真实的API密钥提交到版本控制系统
# 示例: sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
LLM_API_KEY=your-api-key-here

# LLM模型名称
# 常用模型:
#   - gpt-4
#   - gpt-3.5-turbo
#   - openai/gpt-oss-20b (本地模型)
#   - qwen/qwen3-4b
LLM_MODEL_NAME=openai/gpt-oss-20b

# ============================================
# LLM参数配置（可选）
# ============================================

# 温度参数 (Temperature)
# 控制模型输出的随机性和多样性
# 范围: 0.0 - 2.0
#   - 较低的值 (如 0.1-0.3): 输出更确定、一致，适合需要精确答案的任务
#   - 中等的值 (如 0.7-1.0): 平衡创造性和一致性，适合一般对话
#   - 较高的值 (如 1.5-2.0): 输出更具创造性、多样性，适合头脑风暴
# 默认: 1.0
LLM_TEMPERATURE=1.0

# Top-P 参数 (核采样)
# 控制考虑的词汇范围，只考虑累积概率达到P的词汇
# 范围: 0.0 - 1.0
#   - 较低的值 (如 0.1-0.5): 只考虑高概率词，输出更保守
#   - 较高的值 (如 0.9-1.0): 考虑更多词汇，输出更多样
# 默认: 1.0 (通常与temperature二选一使用)
LLM_TOP_P=1.0

# Top-K 参数
# 只保留概率最高的K个词汇进行采样
# 范围: 1 - 100+
#   - 较小的值 (如 10-20): 只考虑最可能的词，输出更确定
#   - 较大的值 (如 50-100): 考虑更多词汇，输出更多样
# 默认: 40
# 注意: 与Top-P类似，通常二选一使用
LLM_TOP_K=40

# 流式输出日志记录间隔
# 每N个delta记录一次日志（控制日志详细程度）
# 范围: 1 - 100+
#   - 1: 记录每个delta（最详细）
#   - 10: 每10个delta记录一次（推荐）
#   - 100+: 减少日志输出
# 默认: 10
LLM_STREAM_CHUNK_SIZE=10

# ============================================
# Agent配置
# ============================================

# Agent插件目录（相对于项目根目录）
PLUGIN_DIR=plugin

# 起始Agent名称
START_AGENT_NAME=entrance_agent

# 结束Agent名称
END_AGENT_NAME=general_agent

# 最大重试次数
MAX_RETRIES=3

# ============================================
# MCP配置（可选）
# ============================================

# 是否启用MCP支持
MCP_ENABLED=false

# MCP配置文件路径（JSON格式）
# 如果设置，将从该文件加载MCP服务器配置
# MCP_CONFIG_FILE=mcp_config.json

# 单个MCP服务器配置（如果不使用配置文件）
# MCP_SERVER_NAME=default_mcp
# MCP_SERVER_COMMAND=npx
# MCP_SERVER_URL=http://localhost:3000/mcp

# ============================================
# 日志配置
# ============================================

# 日志级别
# 可选值: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 日志文件路径（可选）
# 留空则只输出到控制台
# LOG_FILE=logs/easyagent.log

# ============================================
# 应用配置
# ============================================

# 应用名称
APP_NAME=easyAgent

# 应用版本
APP_VERSION=0.1.1

# 调试模式
DEBUG=false
